/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.prismic.io",
      "landingpage-trial.cdn.prismic.io",
      "images.unsplash.com"
    ],
  },
};

export default nextConfig;